/*  function Avatar(props) {
    let person = props.person;
    let size = props.size;
  
    console.log(person); 
    console.log(size);
  
    return (
      <img
        className="avatar"
        src={person.imageUrl} 
        alt={person.name}
        style={{ width: size, height: size }} 
      />
    );
  }  */

function form(props){
    console.log(props)
}